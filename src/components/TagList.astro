---
import { slugifyTerm } from '../utils/blog';
import { withBase } from '../utils/links';

interface Props {
	tags: string[];
}

const { tags } = Astro.props;
---

<!--
	BLOQUE: Lista de etiquetas enlazadas
	- Convierte una lista simple de strings en navegacion tematica.
	- Reusa `slugifyTerm()` para asegurar que las URLs coincidan con las rutas generadas.
-->
{
	tags.length > 0 && (
		<ul class="tag-list" aria-label="Etiquetas">
			{tags.map((tag) => (
				<li>
					<a href={withBase(`/temas/${slugifyTerm(tag)}/`)}>{tag}</a>
				</li>
			))}
		</ul>
	)
}
