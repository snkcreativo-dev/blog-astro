name: Deploy Astro Blog

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # BLOQUE: Se descarga el contenido del repo para que el workflow pueda instalar y compilar.
      - name: Checkout
        uses: actions/checkout@v4

      # BLOQUE: Prepara Node en una version coherente con el proyecto local.
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # BLOQUE: GitHub Pages necesita este paso para reservar el entorno de despliegue.
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      # BLOQUE: Resuelve la URL publica final.
      # - Si el repo es de proyecto, suele desplegarse en /nombre-del-repo
      # - Si fuera un repo tipo usuario.github.io, iria en la raiz del dominio
      - name: Resolve public URL
        shell: bash
        run: |
          if [[ -n "${{ vars.SITE_URL }}" ]]; then
            echo "SITE_URL=${{ vars.SITE_URL }}" >> "$GITHUB_ENV"
          else
            REPO_NAME="${GITHUB_REPOSITORY#*/}"
            OWNER="${GITHUB_REPOSITORY_OWNER}"

            if [[ "$REPO_NAME" == "${OWNER}.github.io" ]]; then
              echo "SITE_URL=https://${OWNER}.github.io" >> "$GITHUB_ENV"
            else
              echo "SITE_URL=https://${OWNER}.github.io/${REPO_NAME}" >> "$GITHUB_ENV"
            fi
          fi

          if [[ -n "${{ vars.BASE_PATH }}" ]]; then
            echo "BASE_PATH=${{ vars.BASE_PATH }}" >> "$GITHUB_ENV"
          fi

      - name: Install dependencies
        run: npm ci

      # BLOQUE: Valida tipos, rutas y colecciones antes de construir.
      - name: Validate project
        run: npm run check

      # BLOQUE: Genera el sitio estatico que despues Pages publicara.
      - name: Build site
        run: npm run build

      # BLOQUE: Sube `dist/` como artefacto intermedio para el job de deploy.
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
